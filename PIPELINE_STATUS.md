# Pipeline Status

- **2026-08-26**: Repo status audit completed. `LFDI-TCB` confirmed as pure STM32F427VIT6 embedded firmware (C, STM32CubeIDE, "SUPER LOOP" in `main.c`, LUTs in `StageDefinitions.h`) matching the Operators Manual description; no overlap with `LFDI-API`'s Python/Flask/calibration code found. Last real activity 2026-02-22 (active, not dormant). See `specs/LFDI_REPO_STATUS_LFDI-TCB_2026-08-26.md` for full findings.
- **2026-08-26**: `upstream/main` (`NCAR/LFDI-TCB`) merged in, including the `BipolarOutput.c` update from PR #8; local history and NCAR are now in sync as of merge commit `90e5245`, pushed directly to `upstream`. `origin` still points at the retired personal fork (`mitchem890/LFDI-TCB`, owner has left the org) — repointing `origin` to `NCAR/LFDI-TCB` is a follow-up cleanup item, not yet done.
- **2026-08-26**: `origin` repointed from the retired `mitchem890/LFDI-TCB` fork to `NCAR/LFDI-TCB` directly; this is now the sole/primary remote. The redundant `upstream` remote (same URL) was removed. A normal `git push`/`git fetch` now goes straight to NCAR, no fork or upstream juggling required.
- **2026-08-26**: Reviewed the full firmware source (`Core/Src`, `Core/Inc`, build config, ~12 months of commit history) and produced a short onboarding reference for new EEs at `docs/onboarding/lfdi_tcb_onboarding_2026-08-26.md` (Markdown, under 4 pages, with two matplotlib-generated diagrams in `docs/onboarding/img/`: module structure and startup/SUPER LOOP execution flow). Covers repo layout, the SUPER LOOP + timer-ISR split, the 5-menu serial protocol (Main/Controller/Compensator/GPIO/BipolarOutput, cross-referenced against `UI.c`), and a first-look punch list. Flagged discrepancies vs. the Operators Manual: (1) firmware has 5 serial menus vs. the manual's documented 3 (GPIO/BipolarOutput added Jan-Feb 2026); (2) `Save`/`Load`/`Update`/`Wipe` are documented in the firmware's own help text but unimplemented stubs (no EEPROM, no persistence — config is lost on every reset); (3) `NUMOFCOMPENSATORS` is defined as 3 but `TCB.Compensator` is a 6-element array and `main.c`'s SUPER LOOP unconditionally calls `Compensator_Update()` on all 6, so slots 3-5 run every pass on a never-initialized struct — needs verification with the firmware maintainer; (4) `CameraController_TriggerCamera()` is an empty stub; (5) `README.md` documents a pre-refactor struct layout (`struct sController`, `NUMOFCONTROLLERS`) that no longer matches the code.
